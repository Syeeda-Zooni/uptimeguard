apiVersion: v1
kind: Service
metadata:
  name: uptimeguard-service
  namespace: uptimeguard
spec:
  type: NodePort
  selector:
    app: uptimeguard
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080
      